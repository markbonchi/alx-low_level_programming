#include "main.h"

/**
 * _islower - function to check for lowercase characters
 * @c: int argument of function for characters
 * Return: 1
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
}
