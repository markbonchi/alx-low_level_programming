#include "main.h"

/**
 * _abs - function to return absolut valure of int
 * @n: argument of function
 * Return: 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
		_putchar(n + '0');
	} else
	{
		_putchar((n * -1) + '0);
	
	return (0);
}
